import 'package:flutter/material.dart';
import 'linuxintegration.dart';
main() {
  runApp(MyApp());

}


class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return myfunc(
      
    );
  }
}